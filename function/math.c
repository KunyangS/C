#include <math.h>

// MATH FUNCTION: allows me to perform mathematical tasks on numbers.
// must include the math.h header file in my program.

// square root function
printf("%f", sqrt(16));

// round a number: ceil() rounds a number upwards to its nearest integer
// round a number: floor() rounds a nunber downwards to its nearest integer
printf("%f", ceil(1.4));
printf("%f", floor(1.4));

// pow: pow() returns the value of x to the power of y(x的y次方)
printf("%f", pow(4,3));